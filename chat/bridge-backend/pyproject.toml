[project]
name = "tvl-bridge-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "tinker>=0.14.0",
    "tinker_cookbook",
    "transformers>=4.57.6,<5.0.0",
    "torch",
]

[tool.uv.sources]
tinker_cookbook = { path = "vendor/tinker-cookbook", editable = true }
