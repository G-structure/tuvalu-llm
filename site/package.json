{
  "name": "talafutipolo",
  "version": "0.1.0",
  "description": "Tuvaluan football news - bilingual EN/TVL",
  "type": "module",
  "scripts": {
    "dev": "vinxi dev",
    "build": "vinxi build",
    "start": "vinxi start",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=futipolo-tv --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=futipolo-tv",
    "deploy:talafutipolo": "npm run build && wrangler pages deploy dist --project-name=talafutipolo --branch=main",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.3",
    "@solidjs/start": "^1.1.0",
    "highlight.js": "^11.11.1",
    "marked": "^17.0.4",
    "solid-js": "^1.9.5",
    "vinxi": "^0.5.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.1",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.19.19",
    "tailwindcss": "^4.0.0",
    "wrangler": "^4.92.0"
  },
  "engines": {
    "node": ">=22"
  }
}
