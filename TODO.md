# TODO

## SOTA Verification

- Run `Helsinki-NLP/opus-mt-tvl-en` on our held-out `test.jsonl`, and run
  `FriezaForce/tvl-en-llm-translation-stage-a` on the OPUS/JW300 test set if we
  can fetch it. Use the shared results to make a clean apples-to-apples SOTA
  claim.
